@using DiplomaWebService.Common.Enum
@using DiplomaWebService.Models.ViewModel
@model ContragentViewModel

@{
}

<div class="app-content">
	<div class="main-header">
		<div class="top-header">
			<h2>Підрозділи</h2>
		</div>
		<div class="btn-header">
			<button type="button" class="btn btn-header btn-header-disable" disabled>Зберегти</button>
		</div>
	</div>
	<nav class="nav">
		@if (@Model.RoleId == (int)Roles.Viewer)
		{
			<button type="add-user" class="btn btn-add-user" onclick="openModalForm('addContragent')" disabled>
				<img src="img/plus.svg">Додати
			</button>
		}
		else
		{
			<button type="add-user" class="btn btn-add-user" onclick="openModalForm('addContragent')">
				<img src="img/plus.svg">Додати
			</button>
		}
		<div class="right-actions">
			<input type="text" class="input-field search-field" name="search" id="search" placeholder="Пошук..." onkeydown="searchContragents(event)">
			@* <img src="img/filtr.svg"> *@
		</div>
	</nav>
</div>
@* <div class="users-table-content my-agent-table"> *@
	<div class="container">
		<div class="block-container ">
			@for (int i = 0; i < @Model.Contragents.Count(); i++)
			{
				if (@Model.Contragents[i].WhoAmI == true)
				{
					<span>Моя в/ ч: @Model.Contragents[i].ContragentName</span>
				}
			}
		</div>
	</div>
@* </div> *@
<div>
	<table class="data-table">
		<thead class="data-table-thead">
			<tr class="data-table-tr">
				<th class="data-table-th" hidden>contragentId</th>
				<th class="data-table-th">Назва</th>
				<th class="data-table-th" hidden>parentId</th>
				<th class="data-table-th">Вище формування</th>
				<th class="data-table-th">Опис</th>
				<th class="data-table-th" hidden>Status</th>
				<th class="data-table-th">Статус</th>
				<th class="data-table-th"></th>
			</tr>
		</thead>
		<tbody id="contragentsList" class="scroll">
			@{
				ViewDataDictionary viewData = new ViewDataDictionary(ViewData);
				viewData.Add("RoleId", Model.RoleId);
			}
			@Html.Partial("/Views/Dictionaries/Contragents/_ContragentsList.cshtml", @Model.Contragents, viewData)
		</tbody>
	</table>
</div>
@Html.Partial("/Views/Forms/ContragentForm/AddContragent.cshtml", Model)
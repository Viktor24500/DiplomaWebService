@using DiplomaWebService.Models.Invoice.In
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<InvoiceIn>

@for (int i = 0; i < Model.Count(); i++)
{
    <tr>
        <td hidden>@Model[i].Id</td>
        <td>@Model[i].InvoiceDate.ToString("yyyy-MM-dd")</td>
        <td>@Model[i].Number</td>
        <td hidden>@Model[i].DestinationId</td>
        <td>@Model[i].DestinationName</td>
        <td hidden>@Model[i].SenderId</td>
        <td>@Model[i].SenderName</td>
        <td hidden>@Model[i].InvoiceType.Id</td>
        <td>@Model[i].InvoiceType.Name</td>
        <td hidden>@Model[i].Sector.Id</td>
        <td>@Model[i].Sector.Name</td>
        <td hidden>@Model[i].DocumentType.Id</td>
        <td>@Model[i].DocumentType.Name</td>
        <td class="td-image">
            <div class="modal-popup">
                <div class="three-dots-button-container">
                    <button onclick="openMenu(@i)">
                        <img src="img/3dots.svg">
                    </button>
                </div>
                <div class="modal-popup-container">
                    <div class="menu-item-container">
                        Перегляд
                        @* <button class="small-icon-container" id="invoiceDetails" onclick="InvoiceInController/invoiceIn/{@Model[i].Id}"><img src="img/review.svg"></button> *@
                        <a href="/invoiceIn/@Model[i].Id" class="small-icon-container">
                            <img src="img/review.svg">
                        </a>
                    </div>
                    <div class="separator">
                    </div>
                    <div class="menu-item-container">
                        Експорт
                        <button class="small-icon-container" id="createExelInvoiceIn" onclick="createReportInvoiceIn(@Model[i].Id)"><img src="img/excel.svg"></button>
                    </div>
                </div>
            </div>
        </td>
    </tr>
}